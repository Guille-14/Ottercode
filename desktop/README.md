# OtterCode desktop (Electron opcional)

Hotkey global sugerido: Ctrl/Cmd+Shift+Space → POST `/api/task`.

Este wrapper no forma parte del runtime FastAPI. La web ya acepta el mismo atajo como fallback.
